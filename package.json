{
  "devDependencies": {
    "@types/node": "^20.10.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "serve dist -s -n -L -p $PORT"
  }
}
